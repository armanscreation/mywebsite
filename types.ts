
export interface ThemeColor {
  name: string;
  value: string;
  bgClass: string;
}

export interface ArmanInfo {
  name: string;
  rank: string;
  status: string;
  skills: string[];
}

export interface ContactInfo {
  email: string;
  github: string;
  location: string;
  availability: string;
}
